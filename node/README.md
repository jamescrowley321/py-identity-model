# identity-model (Node/TypeScript)

**Planned — not yet implemented.**

A native Node/TypeScript library is on the roadmap to join the Python, Go, and
Rust libraries under one shared conformance contract. This directory is a
reserved placeholder; it contains no source and is excluded from all builds and
releases.
