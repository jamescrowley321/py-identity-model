# Provider profile: local Duende IdentityServer fixture (infra/, port 9001).
# Used by `make test-integration-go` (second leg); matches infra/identityserver/Program.cs.
# The Python suite does not target this profile — its known IdentityServer
# capability gaps are documented in docs/identityserver-gaps.md (measured
# against the separate examples/ IdentityServer stack, same product).
TEST_DISCO_ADDRESS=http://localhost:9001/.well-known/openid-configuration
TEST_CLIENT_ID=test-client-credentials
TEST_CLIENT_SECRET=test-client-credentials-secret
TEST_SCOPE=api
TEST_PKCE_PUBLIC_CLIENT_ID=test-pkce-public
TEST_REDIRECT_URI=http://localhost:8080/callback
